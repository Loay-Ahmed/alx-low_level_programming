#include "main.h"

/**
 * print_rev - prints a string in reverse to stdout
 *
 * @s: pointer to the string
 * Return: nothing
 */
void print_rev(char *s)
{
	s += _stlen(*s);
	while ()
	{
		_putchar(*s);
		s--;
	}
}
