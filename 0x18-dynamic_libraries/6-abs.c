#include "main.h"

/**
 * _abs - return postive value of integer
 *
 * @n: number to be checked
 * Return: Alwasy return positive number
 */
int _abs(int n)
{
	if (n < 0)
	{
		return (-n);
	}
	return (n);
}
